// Import spot client at the top
import { RestClientV5 } from 'bybit-api';

// initialise the client
const client = new RestClientV5({
  key: 'insert api key here',
  secret: 'insert api secret here',
});
